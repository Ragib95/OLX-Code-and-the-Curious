Requirements:-
1) Python 2.7
2) In-bulid library used:-
       a) CSV
       b) collections
There is no requirement of external libarary.

Steps for running the program:-
1) First of all drop "ads_data.csv" & "user_data.csv" into "olx-ads-recommendation-dataset" directory, do not replace anything from it.
2) Fire up command prompt.
3) cd into directory containing 'olx.py'
4) Simply type 'python olax.py' and press Enter.

After successfully running of program (approx. running time [Finished in 41.0s]) you can see desired 'ads_recommendation.csv'.